<?xml version="1.0" encoding="UTF-8"?>
<sqls>
    <standard>
        <insert>
            <sql>
                insert into table1(field1,field2) values(value1,value2)
            </sql>
            <sql>
                insert into table1(field1,field2) values(value1,value2)
            </sql>
        </insert>
        <delete>
            <sql>delete from table1 where 范围</sql>
        </delete>
        <update>
            <sql>
                update table1 set field1=value1 where 范围
            </sql>
        </update>
        <select>
            <sql>select * from table as t where t.id=值</sql>
        </select>
    </standard>
    <mysql>
        <addtable>
            <sql>
                create table food
                (
                id int not null auto_increment,
                remark varchar(20) not null unique,
                primary key(id)
                )engine=innodb default charset=utf8;
            </sql>
            <sql>
                create table food
                (
                id int not null auto_increment,
                remark varchar(20) not null unique,
                primary key(id)
                )engine=myisam default charset=utf8;
            </sql>
        </addtable>

    </mysql>
    <oracle>

    </oracle>
</sqls>